name: Build and Deploy (FE to S3 & BE to Lambda)

on:
  push:
    branches:
      - main

env:
  AWS_REGION: us-east-1
  LAMBDA_FUNCTION_NAME: airbrake-backend

jobs:
  # ==========================================
  # FRONTEND DEPLOYMENT TO AWS S3
  # ==========================================
  deploy-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install Frontend dependencies
        run: npm install

      - name: Build Frontend
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync files to S3 bucket
        run: |
          aws s3 sync ./dist s3://airbrake --delete --cache-control "public, max-age=31536000, immutable" --exclude "index.html"
          aws s3 cp ./dist/index.html s3://airbrake/index.html --cache-control "no-cache, no-store, must-revalidate"
      
      - name: Configure S3 bucket for SPA routing
        run: |
          aws s3 website s3://airbrake --index-document index.html --error-document index.html
      
      - name: Verify S3 website configuration
        run: |
          echo "S3 Website Configuration:"
          aws s3api get-bucket-website --bucket airbrake

  # ==========================================
  # BACKEND DEPLOYMENT TO AWS LAMBDA
  # ==========================================
  deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ── Python 3.12 — best compatibility with psycopg2, pinecone, numpy, boto3 ──
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # ── Diagnostics ──────────────────────────────────────────────────────────
      - name: Print environment info
        run: |
          python --version
          python -m pip --version

      # ── Install dependencies into ./package ──────────────────────────────────
      # Failure here stops the workflow — Lambda is never updated.
      - name: Install Python dependencies
        working-directory: ./backend-python
        run: |
          python -m pip install --upgrade pip
          mkdir -p package
          pip install \
            --requirement requirements.txt \
            --target ./package \
            --no-cache-dir
          echo "--- Installed packages ---"
          pip list

      # ── Copy complete backend source into ./package ───────────────────────────
      # Copies all .py files, the full ai/ directory, and any other source.
      # Failure here stops the workflow — Lambda is never updated.
      - name: Copy backend source into package
        working-directory: ./backend-python
        run: |
          # Root-level Python files
          cp app.py             package/
          cp db.py              package/
          cp lambda_function.py package/

          # Complete ai/ package (knowledge_base, recommendations, embeddings, llm, etc.)
          cp -r ai/ package/ai/

          # Jira OAuth integration package (Phase 1)
          cp -r jira/ package/jira/

          # Auth package (local username+password / session management)
          cp -r auth/ package/auth/

          # Verify ai/ made it in with all expected modules
          echo "--- Contents of package/ai/ ---"
          ls -la package/ai/

          # Verify jira/ made it in
          echo "--- Contents of package/jira/ ---"
          ls -la package/jira/

          # Verify auth/ made it in
          echo "--- Contents of package/auth/ ---"
          ls -la package/auth/

          # Fail explicitly if any critical file is missing
          for f in \
            package/app.py \
            package/db.py \
            package/lambda_function.py \
            package/ai/__init__.py \
            package/ai/knowledge_base.py \
            package/ai/recommendations.py \
            package/ai/embeddings.py \
            package/ai/llm.py \
            package/ai/bedrock_embeddings.py \
            package/ai/bedrock_llm.py \
            package/ai/pinecone_service.py \
            package/ai/diagnostics.py \
            package/jira/__init__.py \
            package/jira/oauth.py \
            package/jira/token_store.py \
            package/jira/client.py \
            package/jira/ticket_service.py \
            package/jira/routes.py \
            package/jira/webhook_handler.py \
            package/jira/jira_sync.py \
            package/jira/nova_extractor.py \
            package/jira/sync_pipeline.py \
            package/auth/__init__.py \
            package/auth/local_auth.py \
            package/auth/users.json \
            package/auth/middleware.py \
            package/auth/routes.py \
            package/auth/session_store.py \
            package/auth/user_store.py; do
            if [ ! -f "$f" ]; then
              echo "MISSING: $f"
              exit 1
            else
              echo "OK: $f"
            fi
          done

      # ── Create lambda.zip from the package directory ──────────────────────────
      # Failure here stops the workflow — Lambda is never updated.
      - name: Create lambda.zip
        working-directory: ./backend-python
        run: |
          cd package
          zip -r ../../lambda.zip . \
            --exclude "*/__pycache__/*" \
            --exclude "*/*.pyc" \
            --exclude "*/.git/*" \
            --exclude "*/.env"
          cd ..

          echo "--- Zip size ---"
          du -sh ../lambda.zip

          echo "--- Verifying ai/ exists inside lambda.zip ---"
          unzip -l ../lambda.zip | grep "^.*ai/" | head -20

          # Fail if ai/ directory is not present in the zip
          if ! unzip -l ../lambda.zip | grep -q "ai/"; then
            echo "ERROR: ai/ directory not found in lambda.zip"
            exit 1
          fi

          echo "--- ai/ verified inside lambda.zip ---"

          echo "--- Verifying auth/ exists inside lambda.zip ---"
          unzip -l ../lambda.zip | grep "^.*auth/" | head -20

          # Fail if auth/ directory is not present in the zip
          if ! unzip -l ../lambda.zip | grep -q "auth/"; then
            echo "ERROR: auth/ directory not found in lambda.zip"
            exit 1
          fi

          echo "--- auth/ verified inside lambda.zip ---"

          # Fail if users.json is not present in the zip — auth cannot work without it
          if ! unzip -l ../lambda.zip | grep -q "auth/users.json"; then
            echo "ERROR: auth/users.json not found in lambda.zip — local auth will fail"
            exit 1
          fi

          echo "--- auth/users.json verified inside lambda.zip ---"

      # ── Configure AWS credentials ─────────────────────────────────────────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # ── Deploy to Lambda — only reached if all previous steps passed ──────────
      - name: Deploy to Lambda
        run: |
          aws lambda update-function-code \
            --function-name ${{ env.LAMBDA_FUNCTION_NAME }} \
            --zip-file fileb://lambda.zip

      - name: Wait for Lambda update to complete
        run: |
          aws lambda wait function-updated \
            --function-name ${{ env.LAMBDA_FUNCTION_NAME }}

      # ── Configure Lambda environment variables ─────────────────────────────────
      # Secrets are stored in GitHub → Settings → Secrets and variables → Actions.
      # This step ensures the Lambda always has the correct env vars after deploy.
      # Non-secret values (endpoints, URLs) are set inline.
      # Secret values come from GitHub Secrets — never exposed in logs.
      - name: Configure Lambda environment variables
        env:
          ATLASSIAN_CLIENT_ID: ${{ secrets.ATLASSIAN_CLIENT_ID }}
          ATLASSIAN_CLIENT_SECRET: ${{ secrets.ATLASSIAN_CLIENT_SECRET }}
        run: |
          aws lambda update-function-configuration \
            --function-name ${{ env.LAMBDA_FUNCTION_NAME }} \
            --environment '{
              "Variables": {
                "DSQL_ENDPOINT": "ezt2bkam5s4kjre73r25easkcu.dsql.us-east-1.on.aws",
                "DSQL_REGION": "us-east-1",
                "APP_ENV": "production",
                "FRONTEND_URL": "http://airbrake.s3-website-us-east-1.amazonaws.com",
                "ALLOWED_ORIGINS": "http://airbrake.s3-website-us-east-1.amazonaws.com",
                "ATLASSIAN_CLIENT_ID": "'"$ATLASSIAN_CLIENT_ID"'",
                "ATLASSIAN_CLIENT_SECRET": "'"$ATLASSIAN_CLIENT_SECRET"'",
                "ATLASSIAN_CALLBACK_URL": "https://l7xnpjosjvyrlx55dxrwdvx5g40okeyd.lambda-url.us-east-1.on.aws/api/jira/callback",
                "JIRA_PROJECT_KEY": "ARGUS"
              }
            }'

      - name: Wait for Lambda configuration update
        run: |
          aws lambda wait function-updated \
            --function-name ${{ env.LAMBDA_FUNCTION_NAME }}

      - name: Done
        run: echo "Backend deployed successfully to Lambda!"
